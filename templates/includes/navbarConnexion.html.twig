    <nav class="navbar navbar-fixed-top navbar-expand-lg navbar-dark bg-primary" id="navFixed">
    {# <nav class="navbar navbar-expand-lg navbar-dark bg-primary" id="navAnim"> #}
    
        <a class="navbar-brand" href="{{ path('home') }}">
            <img id="whiteLogo" src="../assets/img/logoWhite.png" alt=""> 
            {# <img id="logo" src="../assets/img/logo.png" alt="">  #}
        </a>
        {# <button class="navbar-toggle ar" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button> #}
        <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav mr-auto">
                <a class="navbar-brand" href="{{ path('home') }}"></a>
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                </li>
            </ul>
        </div>
    </nav>
    </nav>
