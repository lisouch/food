<nav class="navbar navbar-fixed-top navbar-expand-lg fixed-top navbar-dark bg-primary" id="navAll">
    <a class="navbar-brand" href="{{ path('home') }}">
        <img id="logo" src=" {{ asset('assets/img/whiteLogo.png') }}" alt="">
    </a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav mr-auto">
            <a class="navbar-brand" href="{{ path('home') }}"></a>
        </ul>
        <ul class="navbar-nav">

        {% if app.user %} 

            <li class="nav-item active">
                <a class="nav-link" href="{{ path('user_show',  {'id': app.user.id}) }}">Mon compte</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('product_new') }}">Publiez votre annonce</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('cart_index') }}">Panier</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Deconnexion</a>
            </li>

        {% else %}

            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
            </li>

        {% endif %}
        </ul>
    </div>
</nav>